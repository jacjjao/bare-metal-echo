{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cmake",
            "label": "CMake build debug",
            "command": "build",
            "targets": [
                "all"
            ],
            "preset": "${command:cmake.activeBuildPresetName}",
            "group": "build",
            "problemMatcher": [],
            "detail": "CMake debug build task"
        }
    ]
}